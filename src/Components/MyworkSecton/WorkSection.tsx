import { SectionContainer } from '../subcomponents/SectionContainer';
export const WorkSection = () => {
  return (
    <section className="my-work" id="work">
      <SectionContainer className="work-container">
        <h2>a</h2>
      </SectionContainer>
    </section>
  );
};
